kind: Service                             
apiVersion: v1
metadata:
  name: aksproject
  labels:
    app: aksproject
spec:
  ports:
    - port: 8080                               
      targetPort: 90                    
      nodePort: 31200
  selector:
    app: aksproject                    
  type: LoadBalancer                   
